import "styled-components"

declare module "styled-components" {
  export interface DefaultTheme {
    colors: {
      bgColor: string
      blue: string
      liteBlue: string
      text: string
      title: string
      purple: string
      darkBlue: string
      gray: string
      orange: string
      liteOrange: string
      red: string
      black: string
      liteGreen: string
    }
  }
}
